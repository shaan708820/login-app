apiVersion: v1
kind: Service
metadata:
  name: login-app-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: login-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}